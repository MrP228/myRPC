# myRPC

Система удаленного вызова команд (Remote Procedure Call) для ОС AstraLinux.
Состоит из клиента (`myRPC-client`) и демона-сервера (`myRPC-server`).

## Сборка
Проект использует `make`. Для сборки всего проекта:
`make all`

## Установка
Сборка deb-пакетов:
`make deb`
